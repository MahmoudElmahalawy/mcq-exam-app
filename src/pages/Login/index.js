import LoginForm from "../../components/LoginForm";

const LoginPage = () => {
	return <LoginForm title={"MCQ Exam Application"} subtitle={"Please login to continue"} />;
};

export default LoginPage;
